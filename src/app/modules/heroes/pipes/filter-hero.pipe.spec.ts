import { FilterHeroPipe } from './filter-hero.pipe';

describe('FilterHeroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
